
<%= bootstrap_form_for [@user, @task] do |f| %>


  <%= f.text_field :title %>

  <%= f.text_area :description %>

  <%= f.datetime_select :due_date %>

  <%= f.check_box :is_completed? %>
  <%= f.submit %>

<% end %>
